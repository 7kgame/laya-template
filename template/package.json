{
  "name": "{{ name }}",
  "version": "{{ version }}",
  "description": "{{ description }}",
  "author": "{{ author }}",
  "private": true,
  "scripts": {
    "update": "", // 依赖文件下载
    "init": "", // 依赖文件移动
    "build": "" // 渠道打包
  },
  "dependencies": {
  },
  "devDependencies": {
  },
  "engines": {
    "node": ">= 4.0.0",
    "npm": ">= 3.0.0"
  }
}
